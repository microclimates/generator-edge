{
  "name": "${pkgName}",
  "pluginName": "${pluginName}",
  "version": "1.0.0",
  "description": "${description}",
  "homepage": "${homepage}",
  "author": {
    "name": "${author.name}",
    "email": "${author.email}",
    "url": "${author.url}"
  },
  "keywords": ${JSON.stringify(keywords)},
  "dependencies": {},
  "devDependencies": {
    "watch": "^1.0.2"
  },
  "engines": {
    "npm": ">= 8.0.0"
  },
  "scripts": {
  },
  "license": ""
}
