{
  "name": "iot-edge-server",
  "version": "1.0.0",
  "description": "A deployment of the iot-edge server",
  "dependencies": {},
  "scripts": {
    "start": "docker-compose up -d && docker-compose exec nginx sh -c 'echo;echo Edge server running on http://$SITE_FQDN:$HTTP_PORT/$SITE_ID;echo'",
    "stop": "docker-compose down -t 1",
    "restart": "docker-compose restart"
  }
}