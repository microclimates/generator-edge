{
  "iot-edge": {
    "site": {
      "id": "${siteId}",
      "name": "${siteName}",
      "TZ": "${TZ}"
    },
    "externalExposure": {
      "httpPort": ${httpPort},
      "mqttPort": ${mqttPort},
      "mqttWebSocketPort": ${mqttWebSocketPort}
    }
  }
}
